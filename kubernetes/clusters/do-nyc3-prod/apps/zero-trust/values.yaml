# Cluster-specific zero-trust configuration for do-nyc3-prod
# Phase 1: Allow rules enabled by default in base chart

# Phase 2: mTLS - enable after verifying mesh enrollment
# peerAuthentication:
#   enabled: true

# Phase 3: Default deny - enable after auditing traffic
# defaultDeny:
#   enabled: true
