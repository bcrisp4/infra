# Cluster metadata
# Update these values when creating a new cluster

name: "{{cluster_name}}"
provider: "{{provider}}"      # hetzner, digitalocean, aws, gcp
region: "{{region}}"          # Provider region code
environment: "{{env}}"        # prod, stg, dev
