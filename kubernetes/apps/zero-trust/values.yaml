# Zero-trust network policies configuration
#
# Note: Cilium policies (including egressDeny) enable enforcement mode for
# matched endpoints, blocking all non-explicitly-allowed traffic. We use
# Istio for zero-trust instead.

# Istio mesh-wide mTLS enforcement
peerAuthentication:
  enabled: false
  mode: STRICT
