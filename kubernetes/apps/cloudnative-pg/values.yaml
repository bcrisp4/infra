# CloudNativePG Operator defaults

cloudnative-pg:
  crds:
    create: true

# Barman Cloud Plugin for S3-compatible backup storage
barmanCloudPlugin:
  enabled: false
  version: "v0.10.0"
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 128Mi
