[project]
name = "infra-pyinfra"
version = "0.1.0"
description = "pyinfra deploys for homelab hosts"
requires-python = ">=3.11"
dependencies = [
    "pyinfra>=3.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
