# Bootstrap state contains sensitive TFC configuration
# NEVER commit these files
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl

# tfvars may contain sensitive values
terraform.tfvars
